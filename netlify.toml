[build]
  base      = "artifacts/ghost-beam"
  command   = "pnpm install && pnpm run build"
  publish   = "dist/public"

[build.environment]
  NODE_ENV  = "production"
  BASE_PATH = "/"

[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
